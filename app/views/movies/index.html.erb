<div>
  <%= link_to 'Add a movie', new_movie_path, target: "_blank" %>

  <div class='container'>

  </div>
</div>

<div class="container mt-5">
  <div class="row">
    <% @movies.sort.reverse.each do |movie| %>
      <div class="col-2 col-m-4 mt-3">
        <div class="card" style="width: 12rem;">
          <img class="card-img-top" src="<%= movie.poster_url%>" alt="Card image cap">
          <div class="card-body d-flex justify-content-between">
            <strong><%= movie.title %></strong>
            <%= movie.rating %>
          </div>
          <%= link_to 'Voir', movie_path(movie), target: "_blank" %>
        </div>
      </div>
    <% end %>
  </div>
</div>


<%= link_to 'Home', root_path %>
